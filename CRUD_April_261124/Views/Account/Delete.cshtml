@model CRUD_April_261124.Models.tbllogin
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <div>
        @using (Html.BeginForm("Delete", "Account", FormMethod.Post))
        {
            @Html.HiddenFor(x => x.Id)
            @Html.HiddenFor(x => x.password)
            <table>
                <tr><td>@Html.LabelFor(x => x.userID)</td><td>@Html.DisplayFor(x => x.userID) @Html.HiddenFor(x => x.userID)</td><td></td></tr>

                <tr><td colspan="3"><input type="submit" value="Submit"> <input type="reset" value="Reset" /></td></tr>
            </table>
        }
    </div>
</body>
</html>
